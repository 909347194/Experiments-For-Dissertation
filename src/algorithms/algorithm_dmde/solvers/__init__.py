# -*- coding: utf-8 -*-
"""solvers — 求解器实现层（DMDE 主求解器 + 论文对比算法）

职责：
    本包承载算法 3.1（离散映射差分求解器）的完整求解流程，
    并提供第三章表 3-5 实验比对所需的各类对比算法实现，
    供 experiments 层统一调度与评估。

子模块清单：
    - dmde_solver.py      : 离散映射差分求解器（算法 3.1 完整流程）
    - baseline_solvers/   : 论文对比算法（用于第三章表 3-5 实验比对）
        - sort_de.py      : 排序差分进化算法（文献[105]）
        - pmx_de.py       : 部分映射交叉差分算法
        - cmtap_ga.py     : 协同多目标遗传算法（文献[65]）

对外接口约定：
    各求解器应提供统一的求解入口（如 solve(instance, ...) -> result），
    以便 experiments 层在相同实验配置下横向对比（表 3-5）。
"""
