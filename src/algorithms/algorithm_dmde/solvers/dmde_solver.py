# -*- coding: utf-8 -*-
"""dmde_solver.py — 离散映射差分求解器（算法 3.1 完整流程）

职责：
    本模块是 DMDE 方法的主求解入口，将基因表征（representation）、
    环境评估（environments）与映射机制串联为算法 3.1 描述的完整
    求解流程，对外输出最终离散调度/指派方案。

具体承担职责（对应算法 3.1）：
    1. 流程编排：
       初始化种群（经 encoder 生成离散三元组基因）→ 正向映射
       (mapper.phi) 进入连续空间 → 差分变异/交叉进化 → 反映射
       (inverse_mapper.phi' + repair_rules 修复) → 评估 → 选择
       迭代，直至满足终止条件。

    2. 参数管理与默认配置：
       集中管理种群规模、缩放因子 F、交叉概率 CR、最大迭代次数等
       关键参数，并支持外部传入覆盖默认值。

    3. 收敛/终止控制：
       依据最大代数或收敛判据终止迭代，返回最优解及过程记录
       （适应度曲线、耗时等），供实验对比与可视化使用。

    4. 统一结果封装：
       将求解结果按统一结构封装（最优离散方案、目标值、历史记录），
       保证与 baseline_solvers 的输出口径一致，便于表 3-5 比对。

对外接口约定（占位，待实现）：
    - solve(instance, config) -> result: 求解主入口。

注：本文件暂不包含具体实现，仅声明模块职责与边界。
"""
