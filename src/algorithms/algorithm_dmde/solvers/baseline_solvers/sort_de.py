# -*- coding: utf-8 -*-
"""sort_de.py — 排序差分进化算法（文献[105]）

职责：
    本模块实现基于排序表示的差分进化算法（SORT-DE），作为第三章
    表 3-5 实验的对比方法之一，验证 DMDE 在离散映射机制上的增益。

具体承担职责：
    1. 排序编码：
       采用工件/任务排序（排列）表示离散解，个体为顺序编码串。

    2. 差分进化算子：
       在排序个体上执行差分变异与交叉（含基于排序的修复处理），
       保证新个体仍为合法排列。

    3. 评估与选择：
       依据与 DMDE 一致的评估函数计算适应度并执行贪婪选择。

对外接口约定（占位，待实现）：
    - solve(instance, config) -> result: 与主求解器一致的求解入口。

注：本文件暂不包含具体实现，仅声明模块职责与边界。
"""
