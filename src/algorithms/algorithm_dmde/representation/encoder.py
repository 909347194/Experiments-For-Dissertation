# -*- coding: utf-8 -*-
"""encoder.py — 统一三元组基因生成器（规则 3.1 - 3.3）

职责：
    本模块是"基因表征层"的入口，负责将业务决策对象统一编码为
    (工件 ID, 设备 ID, 优先级/次序) 形式的三元组基因串，是整个
    DMDE 离散-连续混合求解流程的起点。

具体承担职责（对应论文规则 3.1 - 3.3）：
    1. 规则 3.1 —— 基因结构建模：
       将调度/指派问题中的每个决策变量规范化映射为三元组 <i, j, k>，
       其中 i 为待加工/待分配工件标识，j 为候选设备标识，
       k 为优先级或加工次序标量，保证所有基因具有统一结构、长度可对齐。

    2. 规则 3.2 —— 合法组合筛选：
       依据实例约束（设备能力、时间窗、资源互斥等），枚举并过滤出
       所有可行的 (i, j, k) 组合，剔除不可行配对，从源头保证
       编码空间与实际可行解空间一一对应。

    3. 规则 3.3 —— 种群初始化：
       基于可行组合按随机/启发式策略批量生成初始种群，为后续
       mapper（正向映射）提供标准的离散基因输入。

对外接口约定（占位，待实现）：
    - encode_triplet(...):  将单个决策对象编码为统一三元组。
    - generate_population(...): 批量生成初始离散基因种群。

注：本文件暂不包含具体实现，仅声明模块职责与边界。
"""
