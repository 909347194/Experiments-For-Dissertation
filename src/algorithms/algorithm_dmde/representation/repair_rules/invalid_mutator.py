# -*- coding: utf-8 -*-
"""invalid_mutator.py — 规则 3.6：越界、重复及无效值随机变异修补

职责：
    作为消解策略的最后兜底，针对反映射与修复后仍残留的非法基因
    （取值越界、索引重复、空/无效占位等），在合法取值范围内执行
    随机变异式修补，将其替换为可行的随机合法值。

具体承担职责（对应规则 3.6）：
    1. 越界修补：将超出定义域的连续值拉回合法区间。
    2. 重复修补：对与既有基因重复的取值做随机再抽取，恢复唯一性。
    3. 无效值修补：将 NaN/Inf/空值等以合法随机值重填。

对外接口约定（占位，待实现）：
    - invalid_mutator(...): 对非法基因执行随机变异修补。

注：本文件暂不包含具体实现，仅声明模块职责与边界。
"""
