# -*- coding: utf-8 -*-
"""inverse_mapper.py — 连续到离散的反映射协调器 phi'（公式 3-7）

职责：
    本模块是"反向还原通道"的协调核心，将连续差分进化算子产生的
    实值个体经 phi' 反映射回离散三元组空间，并统一调度 repair_rules
    下的各消解策略，确保输出的每个离散个体都是合法、可评估的。

具体承担职责（对应公式 3-7）：
    1. 连续 → 离散反映射 phi'：
       依据公式 3-7 将连续坐标映射回离散取值，恢复 (i, j, k) 三元组
       的基本结构，作为对 mapper 正向映射的逆运算主入口。

    2. 冲突协调与策略路由：
       对反映射后出现的离散冲突（多对一抢占、空位、越界、重复等）
       进行分类识别，并将各类冲突分派给 repair_rules 中对应的
       消解策略执行修补，形成"反映射 → 识别 → 消解"的闭环。

    3. 合法性终检：
       在返回前对修复后的个体执行最终一致性校验（与规则 3.2 的
       可行组合集合比对），仅放行满足全部约束的离散解。

对外接口约定（占位，待实现）：
    - inverse_phi(continuous_vector) -> discrete_gene: 反映射主入口。
    - repair_discrete(gene): 冲突协调与修补流程编排函数。

注：本文件暂不包含具体实现，仅声明模块职责与边界。
"""
